const labels = {
  CONFIRM_LEAVE:
    'Are you sure you want to leave?  <br><br>The world will stay the same as you have left it.',
  Button: {
    CONFIRM: 'Yes',
    CANCEL: 'Cancel',
  },
};

// @ts-ignore
window.getLabels = () => {
  return labels;
};
